# Temporary test-branch workflow marker; production runner is on main.
name: Temporary Alabama State Parks Dynamic Test

on:
  push:
    paths:
      - '.github/workflows/temp-alabama-stateparks-test.yml'

permissions:
  contents: write

jobs:
  marker:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Alabama conversion is validated by the temporary main-branch PR workflow."
